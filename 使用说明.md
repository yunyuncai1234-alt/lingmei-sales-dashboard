# 翎美 BI 表格同步版

本版本保留现有公开看板的版式、颜色和模块顺序，并补齐了 Excel → 数据后台 → 公开 BI 的同步链路。

## 日常更新数据

1. 打开 `admin.html`，使用现有管理员账号登录。
2. 首次使用先点击“下载当前数据表”，得到 `翎美BI数据模板.xlsx`。
3. 在 Excel 中修改数据。不要修改 Sheet 名；Sheet 名对应后台左侧显示的英文 key，例如 `summary`、`platform`、`monthly`。
4. 回到数据后台，点击“导入整份 Excel”，选择修改后的文件并确认覆盖。
5. 公开看板会自动读取最新数据。已打开的页面最多约 10 秒完成更新，也可直接刷新页面。

## 表格规则

- 每个 Sheet 的第一行必须是表头，第二行开始是数据。
- 百分比列可以填写 `18%`、`0.18` 或 `18`，导入时会统一为百分比显示。
- 不要合并单元格，不要在数据区域插入空表头。
- 可只保留需要更新的 Sheet；没有包含在 Excel 中的板块不会被覆盖。
- Excel 中无法匹配后台 key 或板块标题的 Sheet 会被跳过，并在导入完成后提示。

## 发布

将本目录中的全部文件上传到 GitHub 仓库根目录并发布 GitHub Pages。新增的 `dashboard-sync.js`、更新后的 `admin.html` 和 `index.html` 必须一起发布。

## 已接入同步的模块

- 顶部汇总指标
- 品类结构
- 平台诊断
- 型号销量
- 分月数据
- F/L 系列年度同比
- 年度投流、当月投流、平台投流明细
- 整体地域数据与地图
- F/L 系列型号地域流向

